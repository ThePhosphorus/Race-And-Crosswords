import { TileColorDirective } from './tile-color.directive';

describe('TileColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TileColorDirective();
    expect(directive).toBeTruthy();
  });
});
